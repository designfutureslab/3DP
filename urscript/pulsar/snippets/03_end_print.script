# Pulsar end-of-print
#
# Stops the daemon, sets both heater targets to 0, closes the socket.
# The robot should already have parked at a safe position before this
# block runs.

pulsar_stop_extrusion()
pulsar_cooldown()
duet_close()
